<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <%= link_to 'DocArk', root_path, class: 'navbar-brand' %>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
          <span class="navbar-text" style="padding-right: 12px">
          <%= link_to t('home'),root_path %>
        </span>
        </li>
        <% if user_signed_in? %>
          <span class="navbar-text" style="padding-right: 12px">
            <%= link_to "#{t('new')} #{t('folder.folder')}",new_folder_path %>
          </span>
        <% end%>
        <% if user_signed_in? %>
          <span class="navbar-text" style="padding-right: 12px">
            <%= link_to "#{t('my')} #{t('folder.folders')}",folders_path %>
          </span>
        <% end%>
      </ul>
      <% if user_signed_in? %>
          <span class="navbar-text" style="padding-right: 12px">
            <%= "#{t('sign_as')} #{current_user.name}" %>
          </span>
          <span class="navbar-text">
            <%= image_tag current_user.image, html_options = {style: 'max-height:40px; border-radius: 25px;'} %>
            <%= link_to(t('logout'), destroy_user_session_path, method: :delete) %>
          </span>
      <% else %>
          <%= link_to t('sign_in_with_google'), user_google_oauth2_omniauth_authorize_path, html_options={class: 'btn btn-primary '} %>
      <% end %>


    </div>
  </div>
</nav>