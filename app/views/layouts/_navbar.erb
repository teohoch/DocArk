<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <a class="navbar-brand" href="http://localhost:3000/">DocArk</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
          <a class="nav-link" href="http://localhost:3000/">
            <%= t 'home' %>
            <span class="sr-only">(current)</span>
          </a>
        </li>
        <span class="navbar-text" style="padding-right: 5px">
          <%= link_to 'New Folder',new_folder_path %>
        </span>
        <% if user_signed_in? %>
          <span class="navbar-text" style="padding-right: 5px">
            <%= link_to 'My Folders',folders_path %>
          </span>
        <% end%>
      </ul>
      <% if user_signed_in? %>
          <span class="navbar-text">
            <%= "#{t('sign_as')} #{current_user.name}" %>
            <%= link_to(t('logout'), destroy_user_session_path, method: :delete) %>
            <%= image_tag current_user.image, html_options = {style: 'max-height:40px; border-radius: 25px;'} %>
          </span>
      <% else %>
          <%= link_to "Sign in with Google", user_google_oauth2_omniauth_authorize_path, html_options={class: 'btn btn-primary '} %>
      <% end %>


    </div>
  </div>
</nav>